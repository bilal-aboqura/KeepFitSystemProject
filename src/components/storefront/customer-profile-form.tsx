"use client";
import { useState } from "react";
import { useRouter } from "next/navigation";
export function CustomerProfileForm() { const router = useRouter(); const [error, setError] = useState(""); const [saving, setSaving] = useState(false); async function submit(form: FormData) { setSaving(true); setError(""); const response = await fetch("/api/customer/me", { method: "PATCH", headers: { "content-type": "application/json" }, body: JSON.stringify({ full_name: form.get("full_name"), phone: form.get("phone") }) }); if (response.ok) router.replace("/account"); else { setError("Please check your name and Egyptian mobile number."); setSaving(false); } } return <form action={submit} className="mt-6 grid gap-4"><label className="grid gap-1 text-sm">Full name<input required name="full_name" className="rounded-lg border border-border bg-transparent p-3" /></label><label className="grid gap-1 text-sm">Mobile number<input required name="phone" inputMode="tel" className="rounded-lg border border-border bg-transparent p-3" /></label>{error && <p role="alert" className="text-sm text-red-400">{error}</p>}<button disabled={saving} className="btn btn-primary justify-center disabled:opacity-60">{saving ? "Saving…" : "Save profile"}</button></form>; }
